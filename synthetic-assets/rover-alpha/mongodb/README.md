# Local MongoDB ingestion

From this directory run `npm install`, start a local MongoDB instance, then run `npm run load`. Defaults are `mongodb://127.0.0.1:27017` and database `forma_rover_demo`.

Override with environment variables `MONGODB_URI` and `MONGODB_DB`. Do not place credentials in this corpus.
