# Synthetic scanner assets

All files in this directory are synthetic demonstrations. The SVGs are polished technical illustrations, not CAD or photographs. The PNGs were generated with the built-in image-generation workflow and are not images of real hardware.

`labels.json` uses pixel-space `[x, y, width, height]` boxes. The six-pin image is a deliberate negative example that must not resolve to J12.
